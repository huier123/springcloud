spring:
  application:
    name: springcloud-consul-producer
  cloud:
    consul:
      discovery:
        service-name: ${spring.application.name}
      host: localhost
      port: 8500
server:
  port: 8400
